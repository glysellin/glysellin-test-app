<%= simple_form_for current_user, html: { class: "form-horizontal" } do |f| %>
  <%= f.input :email, input_html: { class: 'input-block-level'} %>
  <%= addresses_fields_for f %>
  <%= f.submit class: 'btn btn-success' %>
<% end %>
